export default function HomePage() {
  return (
    <main>
      <section>
        <p className="eyebrow">Engagement Tools</p>
        <h1>The dashboard foundation is ready.</h1>
        <p>
          Add authentication, administration, campaigns, and reporting as
          isolated feature modules.
        </p>
      </section>
    </main>
  );
}
